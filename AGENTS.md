Use sub agents to minimize context exhaustion.
Fetch up to date sources on the internet using WebSearch or WebFetch.
Don't make design decisions without asking the user first.
Nothing in this codebase is production. It's just exploration. So don't bother with deprecation or legacy support.
